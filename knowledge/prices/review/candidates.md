# Prices candidates

The bounded bootstrap contains no unrecorded candidate identities. The
accepted oracle evidence review and oracle re-verification review sets cover:

- `source.skip-price-oracle-v1` and `feed.skip-btc-usd` on Mezo mainnet/testnet;
- `source.pyth-core-v1`, `feed.pyth-btc-usd`, and `feed.pyth-musd-usd` on both networks;
- the accepted Skip deployments/ABI and accepted post-upgrade Pyth
  deployments/ABI for bounded registry-only use;
- the provider-neutral taxonomy and deterministic datum/freshness/fallback rules.

The acceptance preserves the exact partial/unverified explorer labels, the
append-only Pyth implementation history, and both generations of fixed-block
stale results. Identity is not liveness.
