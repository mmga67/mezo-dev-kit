<!-- Generated by scripts/generate-incentives-reference.ts. Do not edit. -->

# Mezo incentives evidence reference

This is a deterministic projection of indexed canonical knowledge, not an independent authority.

- Module: `protocols/incentives`
- Support: `none`
- Review: `accepted`
- Verified: `2026-08-24T21:59:22.983Z`
- Review after: `2026-09-22T00:00:00Z`
- Evidence block: `11366264` on `mezo-mainnet`
- Input digest: `sha256:79c1631ac3022fbca112fe4343f1190b285967047981378fbb1c8e03544d1920`

## Inventory

| Resource | Count |
| --- | --- |
| Lock assets | 2 |
| Lock formulas | 4 |
| Gauge types | 4 |
| Emission formulas | 4 |
| Validator formulas | 5 |
| Operations | 12 |
| Formula fixtures | 41 |
| Stable contract roles | 15 |
| Topology evidence roles | 5 |
| Emission topology evidence roles | 8 |
| Dynamic validator gauges at validator evidence block | 24 |
| Source reproduction records | 4 |
| Historical writer integrations | 2 |

## Lock assets

| Role | Maximum lock | Boostable |
| --- | --- | --- |
| vebtc-current | 4 weeks | true |
| vemezo-current | 208 weeks | false |

## Formula boundary

| Formula | Expression / rule |
| --- | --- |
| rounded-unlock-time | floor((timestamp + requestedDuration) / weekSeconds) * weekSeconds |
| unboosted-timed-voting-power | slope = floor(amount / maxLockSeconds); power = at >= lockEnd ? 0 : slope * (lockEnd - at) |
| boosted-timed-voting-power | boostedAmount = boost == 0 ? amount : floor(amount * boost / boostPrecision); virtualSlope = floor(boostedAmount / maxLockSeconds); power = at >= lockEnd ? 0 : virtualSlope * (lockEnd - at) |
| permanent-voting-power | unboosted power = amount; boosted power = boost == 0 ? amount : floor(amount * boost / boostPrecision) |
| vebtc-boost-factor | votingRatio = votingVeTotalWeight == 0 ? 0 : floor(gaugeWeight * precision / votingVeTotalWeight); boostableRatio = boostableVeWeight == 0 ? 0 : floor(boostableVeTotalWeight * precision / boostableVeWeight); boostFraction = floor(4 * boostableRatio * votingRatio / precision); boost = min(maximumBoost, minimumBoost + boostFraction) |
| emission-global-index | On splitter notification: ratio = floor(amount * 1e18 / max(totalWeight, 1)); add ratio to global index only when ratio > 0. |
| gauge-emission-share | On update: delta = globalIndex - gaugeSupplyIndex; share = floor(poolWeight * delta / 1e18). A live gauge accrues share; a killed gauge returns share to the splitter. |
| mezo-epoch-emission | halvingPeriod = floor(epochIndex / 104); progress18 = floor((epochIndex mod 104) * 1e18 / 104); startRate18 = max(floor(2500 * 1e18 / 2^halvingPeriod), 200 * 1e18); endRate18 = max(floor(startRate18 / 2), 200 * 1e18); rateDrop18 = floor((startRate18 - endRate18) * progress18 / 1e18); rate18 = startRate18 - rateDrop18; epochRate18 = floor(rate18 / 52); emissionRaw = floor(floor(epochRate18 * preEmissionTotalSupplyRaw / 10000) / 1e18) |
| mezo-epoch-rebase | Return 0 when emissions are disabled, emissionRaw is 0, preEmissionTotalSupplyRaw is 0, or period is 0.; veVotingPowerRaw = veMEZO.unboostedTotalVotingPowerAt(period - 1); Return 0 when veVotingPowerRaw >= preEmissionTotalSupplyRaw.; nonVotingSupplyRaw = preEmissionTotalSupplyRaw - veVotingPowerRaw; firstRatioProduct = floor(emissionRaw * nonVotingSupplyRaw / preEmissionTotalSupplyRaw); squaredRatioProduct = floor(firstRatioProduct * nonVotingSupplyRaw / preEmissionTotalSupplyRaw); rebaseRaw = floor(squaredRatioProduct / 2) |
| mezo-epoch-reward-remainder | rewardsRaw = emissionRaw - rebaseRaw |
| mezo-splitter-allocation | firstRecipientAmountRaw = floor(currentBalanceRaw * needle / 100); secondRecipientAmountRaw = currentBalanceRaw - firstRecipientAmountRaw |
| validator-vote-allocation | externalWeightTotal = sum(externalWeightRaw[i]); gaugeVoteRaw[i] = floor(externalWeightRaw[i] * boostedVotingPowerRaw / externalWeightTotal); usedWeightRaw = sum(gaugeVoteRaw[i]); allocationFloorDustRaw = boostedVotingPowerRaw - usedWeightRaw |
| validator-reward-index-notification | indexDelta18 = floor(notifiedRewardRaw * 1e18 / max(preNotificationTotalWeightRaw, 1)); globalIndex increases by indexDelta18 only when indexDelta18 > 0 |
| validator-gauge-index-accrual | indexDelta18 = globalIndex18 - gaugeSupplyIndex18; gaugeShareRaw = floor(gaugeWeightRaw * indexDelta18 / 1e18); Advance gaugeSupplyIndex18 to globalIndex18; Add gaugeShareRaw to claimable only while the gauge is alive; otherwise return the share to the splitter |
| validator-gauge-distribution-gate | Update the gauge's index accrual before reading claimable.; Distribute only when updatedClaimableRaw > gaugeLeftRaw and updatedClaimableRaw > 604800.; Set voter claimable to zero, transfer the exact amount to the gauge, and emit DistributeReward with that actual amount. |
| validator-gauge-reward-rate | nextEpoch = timestamp - (timestamp mod 604800) + 604800; timeUntilNext = nextEpoch - timestamp; leftoverRaw = timestamp < priorPeriodFinish ? (priorPeriodFinish - timestamp) * priorRewardRateRaw : 0; newRewardRateRaw = floor((distributionAmountRaw + leftoverRaw) / timeUntilNext); periodFinish = nextEpoch |

## Emission accounting boundary

The active minter creates one epoch emission budget. Rebase and reward amounts partition that budget; they are transfers to separate destinations, not additive mints. Splitter needles are governed state and must be read for the relevant pre-state.

The specification describes locked veMEZO supply, while the active minter uses prior-boundary unboosted, time-decayed voting power. The deployed input and exact integer order govern this version-scoped model; the disagreement remains recorded as a conflict.

## Validator allocation boundary

At block `11366264`, 24 dynamic validator gauges sum exactly to voter `totalWeight`. Current weights remain live persistent state; they are not settled reward amounts.

The bounded history contains 1145 relevant voter events. The representative reward notification floors 405 raw MEZO units across gauge-index shares; its actual event amount remains authoritative.

Validator voting is independent from pool voting. Raw locked BTC, unboosted veBTC power, boosted veBTC power, external relative vote inputs, resulting gauge weights, indexed claimable amounts, distributed MEZO, and claimed MEZO remain separate typed values.

The current official Validator Gauge guide agrees with persistent per-validator voting, while another pinned official validator guide says rewards are equal and not vote-directed. The active executable plus event/state evidence governs the current generation; the disagreement remains explicit documentation drift.

## Deployment and writer boundary

Fifteen deployed roles resolve through accepted Contracts deployment and ABI records with activation histories. validator evidence review accepted the two reusable validator factory roots; their dynamic children remain evidence-scoped instances rather than static registry identities. Registry and protocol review acceptance create no incentives-operation, reader, or writer support.

All twelve operation records describe verified state-machine behavior, but module support is `none`. Historical create-lock and replacement-vote replay proves only those calls at their exact historical pre-states.

Remaining public-capability blockers:

- fresh writer precondition and exact-call verification for any future supported operation

APR, APY, forecasts, and user yield are analytics and are not protocol outputs.
