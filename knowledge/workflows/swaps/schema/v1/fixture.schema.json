{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Mezo swap route fixtures v1",
  "allOf": [{ "$ref": "../../../../schema/v0.4/common-envelope.schema.json" }],
  "type": "object",
  "required": ["records"],
  "properties": {
    "kind": { "const": "mezo-swap-route-fixtures" },
    "owner": { "const": "workflows/swaps" },
    "records": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "object", "required": ["id", "case", "input", "expected"] }
    }
  }
}
