/** These are application choices, not protocol parameters or a return forecast. */
export const swapConfig = {
  amount: "20",
  slippageBps: 50n,
  deadlineSeconds: 300n,
  maxAgeBlocks: 2n,
} as const;
