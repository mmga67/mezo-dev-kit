# Native Bridge evidence reference

The maintained provider and route reference is generated from module
`workflows/bridges`:

- [Bridge evidence reference](../../../knowledge/workflows/bridges/generated/reference.md)

Resolve resources `bridge-routes`, `bridge-contract-roles`,
`bridge-native-lifecycle`, and `bridge-native-evidence` for exact
machine-readable scope. Both recorded Native directions remain evidence
verified but unsupported. Inbound sequence/system progress is not ERC-20
delivery without recipient post-state or equivalent proof. The August 18 Mezo
wrapper observation is historical. A post-v13 observation proves unchanged
wrapper-v6 bytecode and execution generation 6 at block 11358000. bridge evidence review
accepted the current Contract record while retaining the earlier generation as
historical provenance; all routes and writers remain unsupported.

This page is a router only. It owns no independent protocol fact.
