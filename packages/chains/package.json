{
  "name": "@mezo-dev-kit/chains",
  "version": "0.0.0-private",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "Private read-only MDK network identity and capability registry generated from canonical knowledge.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "node ../../scripts/clean-package-dist.ts chains && tsc --project tsconfig.build.json",
    "check": "pnpm format:check && pnpm generate:check && pnpm typecheck && pnpm lint && pnpm build && pnpm test",
    "format:check": "prettier --check src test README.md vitest.config.ts package.json tsconfig.json tsconfig.build.json",
    "generate": "node ../../scripts/generate-chains-package.ts",
    "generate:check": "node ../../scripts/generate-chains-package.ts --check",
    "lint": "eslint src test vitest.config.ts",
    "test": "vitest run",
    "test:shuffle": "vitest run --sequence.shuffle --sequence.seed=56011",
    "test:watch": "vitest",
    "typecheck": "tsc --project tsconfig.json"
  },
  "devDependencies": {
    "vitest": "4.1.11"
  }
}
